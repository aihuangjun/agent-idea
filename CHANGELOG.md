# 变更记录

版本号的唯一事实来源是 `VERSION`，由 `scripts/release.sh` 维护。每个发布版本在这里必须有一节，
`release.sh` 会把该节正文写进更新清单，作为「有新版本」提示里的说明。

## 0.6.2 (2026-09-04)

- 修复：更新装好后点「立即重启」没有反应。原因有两层：对话框还挂在窗口上时 `terminate` 会被系统直接取消；即使收掉对话框，交给 SwiftUI 默认的「能不能退出」判定也会石沉大海。现在应用自己回答可以退出，先收对话框再退，实测 1 秒内退出并拉起新版本。重启脚本等不到旧进程退出就不再去 `open`（那只会把旧实例激活一下）。

## 0.6.1 (2026-09-04)

- 修复：本地构建（版本号带 `-debug`）在「检查更新…」里遇到同一个版本号的正式发布时显示「已是最新」，升不上去。现在同版本号的正式包对本地构建来说也算有更新。

## 0.6.0 (2026-09-04)

- 目录树里拖拽移动文件 / 目录（IDEA 的 Move，手感照访达）：拖影是图标加名字的小块；拖到目录上进那个目录，拖到文件上进文件所在的目录，拖到最上面的项目名上回到根目录，会接收的那个目录圆角描边高亮；在折叠的目录上停一会儿自动展开，拖到列表上下边缘自动滚动；不能放的（同目录、拖进自己、目标已有同名）光标显示不允许。松手就搬、不弹确认，状态栏给一条「已移动 … 撤销」（8 秒后自己消失）；已跟踪的走 `git mv`，开着的标签、后退 / 前进历史跟着换路径，搬完在新位置展开并选中。
- 目录树的按下 / 松开 / 拖拽改为 AppKit 交互层，单击选中、双击打开、箭头展开的手感不变。
- 重命名 / 移动过的文件（git 里的 renamed）在目录树与变更列表里改为蓝色（IDEA 一样按「修改」显示），不再是与新增的绿放在一起像两种绿的青绿。

## 0.5.0 (2026-09-03)

- 目录树里重命名文件 / 目录（IDEA 的 Rename）：右键「重命名…」或选中后按 ⇧F6，对话框打开时选中的是扩展名之前那一段，边敲边校验（空名、含 `/`、同名已存在），回车确认。已跟踪的文件走 `git mv`，变更列表里显示成一条「重命名」；未跟踪的直接搬。开着的标签（含目录下的）、后退 / 前进历史、选中项、展开状态都跟着换到新路径；改名前先把没保存的改动写盘。
- 修复：⌘F 或点放大镜打开查找文件时，焦点常常还留在编辑器里，要再点一下搜索框才能敲字。现在一打开就在搜索框里。

## 0.4.0 (2026-09-03)

- 上一处 / 下一处变更（⇧F7 / F7，IDEA 同款；diff 与有变更的文件标签的标题条右侧也有上下箭头）：编辑器里从光标位置往前 / 往后找一处改动，光标放到那一行并滚到正中；只读 diff（历史提交的、已删除文件的）按连续的变更行段落跳，跳到的那一段左侧有蓝条。到头了那个方向的箭头灰掉。
- 修复：变更列表里点过的文件一直保持高亮，多点几个就全亮着（0.3.0 引入）。

## 0.3.0 (2026-09-03)

- 工作区变更的 diff 可以直接改（IDEA 一样）：并排视图左边是 HEAD 只读、右边是工作区文件，chunk 之间有连接带和「撤回这一块」按钮；单列视图就是文件本身的编辑器，新增行带底色、被删的行以只读小块嵌在原位。同一个文件的 diff 标签与文件标签共用一份未保存内容，⌘S / 自动保存一样生效。已删除的文件、二进制、超过 2MB 的仍是只读 diff；历史提交的 diff 只读。
- 编辑器行号旁有 IDEA 那样的变更标记：与 HEAD 比，改过的行蓝条、新增的行绿条（边敲边更新，提交后基线随 HEAD 换）。
- 文件可编辑：代码与 Markdown 源码在 CodeMirror 编辑器里改（按语言高亮、括号匹配、自动缩进），⌘S 保存，切到别的应用、关标签、退出时自动保存（IDEA 的做法）；改过的标签有蓝点；保存保留原文件的编码与行尾。Markdown 多了「分栏」（左编辑右预览）。超过 2MB 的文件只读。别人在磁盘上改了你正在改的文件会在标题条提示。
- 提交历史：右键某个文件变更「回滚这个变更…」，把那次提交对它的改动反向打回工作区（不产生提交）。
- 后退 / 前进（⌥← / ⌥→，标题条左侧也有按钮）：按打开顺序在标签间来回。
- 查找文件的快捷键改为 ⌘F。
- 右键 `.sh` 等脚本「在终端中运行」（目录树、标签都有）。
- 修复：Agent 已经 `git mv` / `git rm` 过的变更提交时报 `pathspec '…' did not match any files`。磁盘上已不存在的路径（删除、重命名的原路径）改用 `git rm --cached --ignore-unmatch` 暂存，不再交给 `git add`。
- 删除文件：目录树右键「删除…」或选中后按 ⌫，文件与目录都可删；变更列表里已跟踪的文件也能删（原来只有未跟踪的）。一律进废纸篓，删前确认，开着的标签自动关掉。
- 提交信息框换成自绘占位符的编辑框：光标与「提交信息」提示从同一位置起笔，不再错位；高度随内容在 3～6 行间伸缩。

## 0.2.1 (2026-09-02)

- 发布与更新改走 GitHub Releases：每版一个 tag（`vX.Y.Z`）、dmg 作附件，仓库里不再存二进制。「检查更新…」读 Releases 接口，用 GitHub 给的 sha256 摘要校验。0.1.0 与 0.2.0 已补发为 Release。
- 修复外部命令偶发等不到退出的问题（不再依赖 `waitUntilExit`），表现是 git 状态偶尔一直转圈。

## 0.2.0 (2026-09-02)

- 提交历史：左侧工具条第三个按钮（⌘9）。列出当前分支的提交（作者、时间、短 hash，可翻页），选中一条看它改了哪些文件，点文件看那次提交里的 diff（标签上标着 hash，与工作区 diff 区分）。有新提交时自动重拉。
- 查找文件：项目视图标题条的放大镜（⇧⌘O）。按文件名 / 路径边敲边搜，支持子序列匹配（`amdl` 找到 `AppModel.swift`），命中字符高亮；↑↓ 选、回车打开并在树上定位、Esc 关闭。索引后台建，跳过 git 忽略的目录。
- 性能：目录树、变更列表、历史列表按下即选中（不再等松开）；小文件同步读，不再闪一下「加载中」；代码视图改成单个 sticky 行号栏（原来每行一个，几千行的文件切进来要卡几百毫秒）；WebView 常驻不再反复挂载；git 刷新的转圈延迟 300ms 再出现，不再每次文件变化都闪。
- 自动换行下每行自带行号，长行折行后行号仍对齐。
- 渲染超过 300ms 会记一条日志（菜单「显示日志…」），方便排查慢在哪。

## 0.1.0 (2026-09-02)

首个正式版。Agent 开发配套的 IDEA：看工作区，看 Agent 改了什么。

- 项目：同时打开多个项目（顶栏标签切换），重启自动恢复；欢迎页记着最近项目；支持拖目录进窗口、`open -a AgentIDEA <目录>`。
- 目录树：IDEA 习惯（单击选中、双击打开/展开），按 git 状态着色（修改蓝、新增绿、删除灰、忽略灰），「定位当前文件」（⌥⌘L）。
- 阅读：代码按语言高亮、Markdown 渲染（mermaid、任务列表、相对图片）、单行 JSON 自动格式化、图片预览；预览/源码切换、自动换行、缩放。
- Git：分支与变更列表；diff 并排/单列两种视图、词级高亮；勾选文件后提交、提交并推送、推送；回滚与删除未跟踪文件。
- 项目目录变化自动刷新（FSEvents），正在看的文件保持滚动位置重载。
- 基础设施：版本/构建标识、日志目录、检查更新（GitHub `releases/`），沿用 KBReader 的工程做法。
